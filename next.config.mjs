/** @type {import('next').NextConfig} */
const nextConfig = {
  // Pas de "output: export" — incompatible avec npm run dev
  // Pas de basePath — on tourne en local sur localhost
};

export default nextConfig;
